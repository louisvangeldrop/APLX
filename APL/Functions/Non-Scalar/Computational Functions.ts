namespace Monadic {

    namespace NonScalar {

        // domino←{(⍴⍵)⍴÷+/⍵}

        //var inverse = (omega) => {return         }

    }

}

namespace Dyadic {

    namespace NonScalar {

        //var divide = (omega) => {return         }

    }
}